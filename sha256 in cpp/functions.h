#ifndef Functions_H
#define Functions_H

#include <iostream>
class Converter{
    public:
        int length;

        Converter(int lengthin){
            const int length = lengthin;
            std::cout << "help" << std::endl;
        };

    //converts an array with hex to an array with binary
    std::array<int,length*4> hex_to_binary(std::array<hex,length> message){
        
        return 0;
    };
};
#endif